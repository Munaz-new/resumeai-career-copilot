# PDF export fix

The builder PDF exporter captures the resume at its actual rendered preview width instead of forcing a 760px export width. This keeps responsive columns, line wrapping, and vertical text positions aligned with the live preview.
